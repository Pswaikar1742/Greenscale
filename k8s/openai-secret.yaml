# OpenAI API Secret for GreenScale
# Owner: P (Platform Engineer)
# ⚠️  WARNING: Update the OPENAI_API_KEY before deploying!

apiVersion: v1
kind: Secret
metadata:
  name: openai-secret
  namespace: greenscale-system
type: Opaque
stringData:
  # ⚠️ REPLACE with your actual OpenAI API key
  OPENAI_API_KEY: "sk-your-openai-api-key-here"
